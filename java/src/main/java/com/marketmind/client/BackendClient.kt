package com.marketmind.client

import org.springframework.stereotype.Component

/**
 * Stub BackendClient — gRPC integration disabled.
 * Python integration uses PythonRunner instead.
 *
 * JAVA → KOTLIN: No-arg class with @Component; `class BackendClient` replaces
 * `public class BackendClient`. No `public` needed — Kotlin defaults to public visibility.
 */
@Component
class BackendClient
// No-body needed — empty primary constructor is implicit when there's nothing to declare
