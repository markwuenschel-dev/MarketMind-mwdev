# Local GitHub API auth for agents and `gh` CLI (copy to `.env`, never commit `.env`).
# Create a fine-grained or classic PAT: https://github.com/settings/tokens
# Suggested scopes: repo (or fine-grained: Contents, Pull requests, Actions read)
GH_TOKEN=
GITHUB_TOKEN=
